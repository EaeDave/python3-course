# if / elif      / else
# se / se não se / se não

condicao = False

if condicao:
    print('Este código só será executado caso a condição seja True.')
else:
    print('Este código só será executado caso a condição seja False.')

print('Fora do bloco.')  # Será executado independentemente, pois não está dentro do bloco condicional.

# Nesta aula foi apresentado o Debugger do VSCode, breakpoints e análise de código.